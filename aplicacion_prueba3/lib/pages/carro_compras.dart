import 'package:flutter/material.dart';

class CarroCompras extends StatelessWidget {
  const CarroCompras({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      );
  }
}