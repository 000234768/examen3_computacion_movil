import 'package:flutter/material.dart';

/* 
Ver productos añadidos, 
quitar productos, 
comprar productos (Revisión del stock disponible, de existir, realizar venta enviando un mail al cliente, 
          rebajando stock, de lo contrario, informar con un mensaje y borrar producto del carro de compras)
*/
class CarroCompras extends StatelessWidget {
  const CarroCompras({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      );
  }
}